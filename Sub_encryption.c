#include <stdio.h>
#include <stdlib.h>

int main()
{
    char text[50]
    printf("Enter text to encrypt (CAPS ONLY):");
    scanf("%s", text);
    
    FILE *input;
    input=*fopen(*File_Encrypt, "r");
        while(!feof(input)) {
            ...
    }
    
}